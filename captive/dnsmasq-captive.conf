# 성적서 AP 캡티브 포털 — 모든 도메인을 AP IP(10.42.0.1)로 돌린다.
# 설치: sudo cp captive/dnsmasq-captive.conf /etc/NetworkManager/dnsmasq-shared.d/
# NetworkManager 의 공유(shared) 모드 dnsmasq 가 이 드롭인을 읽는다.
# AP 망(10.42.0.x)의 폰만 영향받는다 — 단말 자신의 인터넷 DNS(wlan0)와 무관.
address=/#/10.42.0.1
